# D-market 引継ぎ書（2026-06-19）

新しい開発セッションがそのまま続行できるための、現状・構成・手順・残作業の完全な引継ぎ。
**まずこの文書を最初に読むこと。** 関連文書: `計画書.md` / `二層ポイント制_設計案.md` / `スケーラビリティ_将来検討.md` / `Dmarket_PolyUIUX改善案.md` / `Bugs_Fix_Log.md`。

---

## 0. 一言で言うと

**D-market**（ディーマーケット）= Polymarket型UXを、**換金不可・無償発行ポイント**で再現した日本語の予測市場。
**賭博非該当**を設計の核に据える（ポイントは無償発行＝ユーザーは自己の財産を賭けない）。
貯めたポイントは将来**景品と確定交換**できる（ミライマ型の二層ポイント制を導入中）。

- 本番URL: **https://dmarket-six.vercel.app/**
- リポジトリ: GitHub `jinjinsansan/dmarket`（main ブランチ。プッシュ即Vercel自動デプロイ）
- 開発ディレクトリ: `E:\dev\Cusor\dmarket\`
- オーナー: 仁さん（goldbenchan@gmail.com）。LINEログイン済・管理者認定済。

---

## 1. 技術スタック

| 層 | 技術 |
|---|---|
| フロント | Next.js 16.2.9 (App Router) / React 19 / TypeScript / Tailwind v4(@theme) / Recharts |
| 認証 | LINEログイン（OAuth2）→ Supabase セッション |
| DB | Supabase Postgres。SECURITY DEFINER plpgsql RPC ＋ RLS（公開SELECTのみ、書込はRPC経由） |
| Realtime | Supabase Realtime（outcomes.q 変更を購読しフロントで価格再計算） |
| ジョブ | Edge Functions(Deno) ＋ pg_cron + pg_net |
| ホスティング | Vercel（web/） |

価格決定は **LMSR**（対数マーケットスコアリング）。`b_param`が流動性。**顧客向けUIに「LMSR」の語は出さない**（「自動で価格が決まる」等に言い換え済み）。

---

## 2. ディレクトリ構成（重要ファイル）

```
dmarket/
├ web/                         ... Next.js アプリ（Vercel デプロイ対象）
│  ├ app/
│  │  ├ page.tsx               ... ホーム（getMarkets/getCategories → MarketGrid）
│  │  ├ layout.tsx             ... ルート。テーマ(dark既定)・フォント・TopNav/Footer/BottomNav
│  │  ├ loading.tsx            ... ホームのスケルトン（グリッド寸法を一致させること）
│  │  ├ market/[id]/page.tsx   ... 市場詳細（force-dynamic, getMarket+getRelatedMarkets）
│  │  ├ legal/                 ... 法的情報4ページ（terms/privacy/tokushoho/no-gambling）
│  │  ├ admin/                 ... 管理画面（dashboard→各メニュー→各ページ）
│  │  └ api/auth/line/         ... LINE OAuth (login/callback/logout)
│  ├ components/
│  │  ├ MarketGrid.tsx         ... ホーム本体。回転ヒーロー＋細カテゴリナビ＋グリッド＋Realtime
│  │  ├ MarketCard.tsx         ... 市場カード（4列・ホバーでprefetch）
│  │  ├ MarketDetailClient.tsx ... 詳細クライアント（大%・タブ・モバイル下部シート）
│  │  ├ MarketTabs.tsx         ... 注文板/保有者/取引履歴/コメント
│  │  ├ TradePanel.tsx         ... 売買パネル
│  │  ├ Footer.tsx / TopNav.tsx / BottomNav.tsx / Sparkline.tsx / LegalHeader.tsx
│  ├ lib/
│  │  ├ queries.ts             ... 公開読み取り（匿名クライアント＋unstable_cacheで短期キャッシュ）
│  │  ├ supabase/server.ts     ... cookie付きサーバークライアント（ユーザー文脈）
│  │  ├ supabase/anon.ts       ... cookie無し匿名クライアント（公開読み取り・キャッシュ可能）
│  │  ├ supabase/client.ts     ... ブラウザクライアント（Realtime/RPC）
│  │  ├ supabase/admin.ts      ... service_role（LINE認証のサーバー処理）
│  │  ├ lmsr.ts / format.ts / market-visual.ts / useAnimatedValue.ts / line.ts
├ supabase/
│  ├ migrations/0001〜0022     ... DBマイグレーション（下記§4）
│  ├ deploy/                   ... SQL Editor貼付け用（full_schema.sql 等）＋ functions/
│  └ tests/                    ... ローカルDBテスト（run_local.sh で一括）
├ specs/                       ... SPEC-00〜08 仕様書
├ 計画書.md / 二層ポイント制_設計案.md / スケーラビリティ_将来検討.md
├ Dmarket.txt                  ... ⚠️ service_role key 等の秘密（gitignore・コミット厳禁）
└ D-market　LINE情報.txt        ... ⚠️ LINE認証情報（gitignore・*.txt除外）
```

---

## 3. ビルド・テスト・デプロイ手順

### フロントのビルド確認（編集後は必ず）
```bash
cd /e/dev/Cusor/dmarket/web && npm run build
# 注意: Bashツールのcwdが root に戻ることがある。必ず web を明示。
# "✓ Compiled successfully" を確認。
```
- AGENTS.md の警告: このNextは破壊的変更あり。キャッシュ等のAPIは `node_modules/next/dist/docs/` を確認。

### ローカルDBテスト（Docker不要・PG16バイナリ使用）
```bash
cd /e/dev/Cusor/dmarket
PG_BIN="/c/Program Files/PostgreSQL/16/bin" bash supabase/tests/run_local.sh
# "ALL TESTS PASSED" を確認。0009_cron はローカル非適用（pg_cron/pg_net必須のため）。
# 新マイグレーションは run_local.sh の migration ループとテスト行に追記すること。
```

### マイグレーションの本番適用（重要）
- **コード変更（web/）はpushで自動デプロイされるが、SQLは手動適用が必要。**
- 手順: Supabaseダッシュボード → SQL Editor → 該当マイグレーションファイルを**VSCodeで開いてコピペ**して実行。
- ⚠️ **チャット経由でSQLを貼らない**（`--`がem-dashに化ける等の事故が頻発）。必ずファイルから。
- DDLの正常応答は「Success. No rows returned」。

### デプロイ（Vercel）
- `git push` で main に上げると Vercel が自動ビルド＆デプロイ。数十秒で反映。
- 仁さんが自分で `! git push` する場合もあるが、これまでは Claude がpushしてきた。

---

## 4. マイグレーション一覧（0001〜0022）

| 番号 | 内容 |
|---|---|
| 0001 | 中核テーブル（wallets/point_ledger/categories/markets/outcomes/positions/resolutions/daily_grants/market_price_history）＋RLS |
| 0002 | LMSR関数（safe_exp でアンダーフロー対策） |
| 0003 | 発行RPC（grant_signup_bonus=1000 / claim_daily_grant=100・JST） |
| 0004 | 売買RPC（buy_shares/sell_shares） |
| 0005 | 解決RPC（resolve_market 勝者一括償還=株×100pt / void_market 返金） |
| 0006 | Realtime（outcomes/market_price_history を publication 追加） |
| 0007 | 供給・解決補助 | 0008 | 市場生成 internal | 0009 | cron（**リモート専用**） |
| 0010 | profiles | 0011 | leaderboard + refresh_user_stats | 0012 | admin(is_admin等) |
| 0013 | 収益化/不正検知（absence test の思想） | 0014 | 管理ダッシュボード | 0015 | 市場生成 display_order 修正 |
| 0016 | LINE認証(profiles.line_user_id + complete_line_signup) | 0017 | 詳細タブ(orders/comments/holders RPC) |
| 0018 | admin_list_users/admin_adjust_points | 0019 | platform_settings/admin_economy/market管理 | 0020 | market_sparklines |
| 0021 | **パフォーマンス索引**（markets(status,close_time) / markets(category_id,status,close_time) / positions(outcome_id)）※適用済 |
| 0022 | **二層ポイント制 Phase A**（賞品pt層）※適用済 |
| 0023 | **二層ポイント制 Phase B**（的中報酬: resolve_market に賞品pt付与フック＋prize_win_rate設定）※適用済 |
| 0024 | **二層ポイント制 Phase C**（景品マスタ/交換申込の管理RPC）※適用済 |
| 0025 | **二層ポイント制 Phase D**（賞品pt失効の日次cron・リモート専用）※適用済（cron.job 登録確認済） |

**不変条件（絶対守る）**: 各ユーザーで `wallets.balance == Σ point_ledger.delta`、`prize_wallets.balance == Σ prize_ledger.delta`。残高はホワイトリストRPCを経由しないと変動しない（賭博非該当の技術的担保）。テストが常時検証する。

---

## 5. 二層ポイント制（現在ここを開発中）★最重要

ミライマ（株式会社Masentic）調査を踏まえ導入決定。設計詳細は `二層ポイント制_設計案.md`。

### 確定した仕様（仁さんの判断）
1. **参加ポイント**（既存 wallets.balance）= 予想の売買に使う価値ゼロ通貨。市場・LMSR・売買・解決はこちらで完結。
2. **賞品ポイント**（新設 prize_wallets）= 景品交換専用。**的中報酬**で付与（予想を当てると貰える）。
3. 現行「ポイント」は **「参加ポイント」に改称**する（Phase Bで実施予定）。
4. 導入は **Phase A→D 段階的**。

### Phase A = 完了済み（0022・テスト全緑・本番適用済み）
- テーブル: `prize_wallets` / `prize_ledger`（FIFO失効用 expires_at 保持）/ `prizes`（景品マスタ）/ `prize_redemptions`（交換申込）
- RPC: `grant_prize_points(p_user,p_amount,p_reason,p_market_id,p_expires_at)`（サーバー専用・有効期限既定90日） /
  `redeem_prize(p_prize_id,p_shipping)`（確定交換・本人・在庫/残高/期限を検証） /
  `expire_prize_points()`（FIFO失効。`max(0, 期限切れ付与合計 − 消費合計)` で正確・二重失効しない）
- RLS: 自分のものだけ読む。prizes は is_active のみ公開。
- 参加pt側は**無改修**。

### Phase B = 一部完了（0023・ローカルテスト全緑・ビルドOK・**本番SQL未適用**）
仁さんの判断: **的中報酬は払戻ベース**＝賞品pt = floor(勝ち株数 × `prize_win_rate`)。既定 rate=1（勝ち1株=1賞品pt）。
- ✅ `resolve_market`（0023で再定義）に的中報酬ブロックを set-based 追加。有効期限 now()+90日。status ガードで冪等（二重付与なし）。
- ✅ `platform_settings.prize_win_rate`（既定1）追加＋`admin_set_setting` ホワイトリストへ追加。管理画面 `/admin/params` で調整可能。
- ✅ マイページ（`web/app/mypage/page.tsx`）に賞品pt残高・履歴・有効期限を表示。残高カードも「参加ポイント/賞品ポイント」の2枚に。
- ✅ ローカルテスト `0008_prize_points.sql` に Phase B（解決→win_reward付与）検証を追加。`run_local.sh` に 0023 を追記。
- ⚠️ **本番未適用**: `supabase/migrations/0023_prize_win_reward.sql` を Supabase SQL Editor にファイルからコピペで実行すること（適用するまで本番の解決では賞品ptは付かない）。

### Phase B「参加ポイント」改称 = 機能UI・管理画面は完了（ビルドOK）
- ✅ ヘッダー残高（`TopNav`）: 残高ピルを「参加pt」表記＋ツールチップに。
- ✅ `TradePanel`: 残高不足メッセージ「参加ポイントが足りません」／中止返金文を「取得した参加ポイント…」に。
- ✅ マイページ（`mypage`）: 残高カードを「参加ポイント／賞品ポイント」2枚に（対応済）。
- ✅ 管理画面: ダッシュボード menu desc / 経済モニタ（`economy`：供給・補助金・理由別内訳を「参加ポイント」明示）/ ユーザー運用（`users`：履歴・付与/消滅を「参加ポイント」明示）/ パラメータ（`params`：的中報酬レート追加）。
- ✅ **法的4ページ＋Footer＋layoutメタも二層化改稿済み**（機械置換ではなく文意を保った改稿）:
  - terms: 第2条定義に参加/賞品ポイントを分離・総称を定義。第4条を二層構成に。第5条で「的中時に賞品pt付与」明記。第6条は賞品ptで景品交換。第7/8/10条も整合。
  - no-gambling: 「賭けに相当する市場予測に用いるのは財産的価値のない参加ポイントだけ／景品に触れるのは的中報酬の賞品ポイントのみ」を明示し賭博非該当の論拠を強化。
  - privacy/tokushoho: 取得情報・利用目的・料金/返還を二層に整合（賞品pt＝景品交換専用）。
  - `MarketDetailClient` の解決ルール文の**旧コピー（「得られるのは称号とランキングだけ」）を修正**（賞品pt→景品の導線を明記）。
  - ⚠️ 文面は**公開前に弁護士レビュー必須**の方針は不変（§6）。会社情報【要記入】も未対応のまま。
### Phase C（景品公開）= 実装ほぼ完了（0024・ローカルテスト全緑・ビルドOK・**本番SQL未適用**）
- ✅ DB 0024: 管理RPC `admin_list_prizes` / `admin_upsert_prize` / `admin_set_prize_active` / `admin_list_redemptions` / `admin_set_redemption_status`（is_admin＋_audit）。取消（未発送のみ）で賞品pt返金＋在庫復元（balance==Σdelta 維持）。
- ✅ 管理画面 `/admin/prizes`: 景品マスタCRUD（名前/説明/画像/必要pt/在庫/公開/表示順）＋交換申込一覧（ステータスフィルタ・承認/発送済/取消、配送先表示）。ダッシュボードにメニュー追加。
- ✅ ユーザー `/prizes`: 景品一覧（カード・在庫・賞品pt残高）＋確定交換モーダル（配送先入力→`redeem_prize`）。TopNav/BottomNav/マイページから導線。
- ✅ テスト `0009_prize_admin.sql` 追加（CRUD・交換→取消返金・不変条件）。run_local に 0024 追記。
- ⚠️ **本番未適用**: `supabase/migrations/0024_prize_admin.sql` を SQL Editor で適用するまで `/admin/prizes` は「admin_list_prizes が無い」エラーになる。
- ⏳ **残: 特商法表記の【要記入】を実会社情報に差し替え**（`web/app/legal/tokushoho/page.tsx` の OPERATOR 定数）。仁さんから会社名・責任者・所在地・電話・メールを受領待ち。
- ⏳ **残: 景品設計確定後の弁護士レビュー**（景表法の景品最高額・総額規制ほか）。
### Phase D（運用）= 失効cron 本番稼働（0025・適用済）
- ✅ DB 0025（リモート専用）: 日次cron `expire-prize-points`（`0 19 * * *` = 04:00 JST・active）で `expire_prize_points()` を直接実行（refresh-stats と同方式・Edge Function/秘密鍵不要）。**cron.job 登録を本番で確認済み**。
- ⏳ 残: 在庫・配送オペ（運用）。特商法の会社情報【要記入】（後回し中）。景品設計確定後の弁護士レビュー。

**→ 二層ポイント制 Phase A〜D の実装はすべて本番稼働で完了。** 以降は運用・公開前項目（特商法の実会社情報・弁護士レビュー・配送オペ）のみ。

---

## 6. 法的情報（実装済み・要確認あり）

`/legal/` 配下に4ページ（Footerの「法的情報」からリンク済み）:
- `terms`（利用規約・全14条＋3条の2年齢＋6条景品/景表法遵守明記）
- `privacy`（プライバシーポリシー）
- `tokushoho`（**特定商取引法表記・会社情報は【要記入】プレースホルダ**）
- `no-gambling`（賭博非該当の説明。**断定を避け「〜生じないよう配慮」トーン**に調整済み＝ミライマ流）

賭博非該当の論拠 = **ポイントは無償発行（現金で買えない。提携懸賞/アフィリエイトサイト登録の無料特典＋デイリー付与）→ ユーザーは自己の財産を賭けていない → 賭博罪の「財産上の利益の得喪」を欠く**。景品交換は確定交換（抽選なし）・無償pt利用先。無償発行ゆえ資金決済法の前払式支払手段にも非該当。

### ⚠️ 公開前に必須
1. 特商法表記の【要記入】（運営会社名・責任者・所在地・電話・メール）を実情報へ。
2. **景品設計が固まったら、賭博・景品表示法・特商法の一連を弁護士レビュー**。景品を出す以上ここは外せない（ミライマも景表法遵守を明記）。

---

## 7. 高負荷対策（実装済み）

詳細は `スケーラビリティ_将来検討.md`。
- 公開読み取りを**匿名クライアント＋unstable_cache**で短期キャッシュ（一覧10s/詳細5s/関連15s/カテゴリ60s）。価格はRealtimeが補正するため鮮度十分。
- 索引3本（0021・適用済）。`getMarkets`に `limit(200)` 安全弁。
- カード/ヒーロー/注目を**ホバー・タップ直前にprefetch**（遷移ラグ軽減）。
- **将来検討（トリガー条件付き）**: DBインスタンス増強 / Realtime購読の絞り込み / 本格ページネーション。実ユーザー増加後に着手。

---

## 8. セキュリティ制約（厳守）

- `Dmarket.txt`（service_role key 等）と `D-market　LINE情報.txt` は **gitignore済・コミット厳禁**（`*.txt`除外）。
- **`SUPABASE_SERVICE_ROLE_KEY` に `NEXT_PUBLIC_` を付けない**（クライアントへ漏れる）。
- `*.local.sql`（cronのkey埋め込み版）はgitignore。
- SQLはチャット貼付け禁止、必ずファイルから（文字化け事故防止）。
- Vercel環境変数: `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY` / `LINE_LOGIN_CHANNEL_ID` / `LINE_LOGIN_CHANNEL_SECRET` / `NEXT_PUBLIC_SITE_URL`（末尾空白に注意。コードで trim 済）。翻訳用に DeepSeek 等のキーも。

---

## 9. 既知の課題・未決定事項

| 区分 | 内容 |
|---|---|
| 決定済 | Phase B の**的中報酬レート**＝払戻ベース floor(勝ち株×prize_win_rate)、既定rate=1。`/admin/params`で調整可。 |
| 未対応 | 特商法表記の会社情報【要記入】。 |
| UI既知課題 | ワールドカップ等、Gamma APIが国ごとの二択市場を返すため**約30枚の国旗カードに分散**。本家は1カードにグループ化。データ＋UIのグループ化が必要（未着手）。 |
| 任意 | §3改善の残り（検索ダイアログ、出来高バー＝DBに volume 列が必要）。 |

---

## 10. 仁さんとの進め方（協業ルール）

- 仕様書ファースト・確認重視。大きな分岐は `AskUserQuestion` で選択肢を出して判断を仰ぐ。
- 「寝る」「明日に」を勝手に提案しない。継続意思があれば時刻に関係なく着手。
- 日本語でやり取り。実装後はビルド/テストで検証してから完了を報告（失敗は隠さず出力を見せる）。

---

## 11. 次の一手（このセッションの続き）

直前まで: 二層ポイント制 **Phase B の主要3点完了**（0023・ローカルテスト全緑・ビルドOK）= 的中報酬フック / 管理レート調整 / マイページ表示。
→ **まず本番に 0023 を SQL Editor で適用**（未適用だと解決で賞品ptが付かない）。
→ 次は **「ポイント」→「参加ポイント」UI一括改称**（TopNav/TradePanel/Footer等）→ Phase C（景品公開・管理CRUD・特商法【要記入】差し替え）。
